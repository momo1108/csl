function solution(numbers) {
    var answer = 0;
    let maxnum = "";
    numbers.toString().split("").sort((a,b)=>b-a).forEach(e=>{
        maxnum += e;
    });
    maxnum = parseInt(maxnum);
    for(let i=2; i<=maxnum; i++){
        for(let j=)
    }
    return answer;
}
solution(15649)