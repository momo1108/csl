function solution(progresses, speeds) {
    var answer = [];
    var temp = [];
    while(1){
        progresses.forEach((e,i) => {
            e += speeds[i];
            if(e>=100)
        });
        
    }
    return answer;
}